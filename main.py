import functions

if __name__ == '__main__':
    functions.printData()
























# Esempio di estrazione delle caratteristiche utilizzando la codifica a caldo (one-hot encoding)
# Rimuovere '.head(5)' per eseguire l'operazione su tutti i dati anziché solo sui primi 5
#encoded_data = pd.get_dummies(data, columns=['url'])
#print(encoded_data.head())

# Oppure, puoi utilizzare altre tecniche di estrazione delle caratteristiche come Word2Vec o FastText
# Assicurati di importare le librerie necessarie e di eseguire le operazioni di pre-elaborazione appropriate per queste tecniche.
